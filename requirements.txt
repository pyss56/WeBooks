Flask==2.3.3
requests>=2.25.1
python-dotenv>=1.0.0
pycryptodome>=3.20.0
schedule>=1.2.0
croniter>=1.3.5
pytz>=2023.3
APScheduler>=3.10.4
# OCR 识别（轻量版，推荐）- 设置环境变量 PIP_AUTO_INSTALL=ddddocr 自动安装
# ddddocr
# OCR 识别（重量版，含 PyTorch ~800MB）
# paddleocr
